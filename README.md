# 🧑‍🎓 Flask Student Management API

This is a simple **Flask-based REST API** that allows you to add student records and prevent duplicate entries based on phone numbers.  
The API stores student data in a local text file (`students.txt`).

---

## 🚀 Features

- Add new students using a POST request  
- Prevent duplicate student entries (based on phone number)  
- Read and store data in a text file (`students.txt`)  
- JSON-based communication between client and server  

---


